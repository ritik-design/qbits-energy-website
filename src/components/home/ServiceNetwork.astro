---
const states = [
  {
    id: 'gujarat', name: 'Gujarat',
    path: 'M180,380 L160,400 L150,420 L170,450 L200,460 L230,450 L240,420 L220,390 Z',
    labelX: 200, labelY: 425,
    centers: ['Surat', 'Ahmedabad', 'Vadodara', 'Rajkot'], partners: 18, distributors: 42,
  },
  {
    id: 'rajasthan', name: 'Rajasthan',
    path: 'M200,200 L180,250 L190,300 L220,340 L260,350 L300,320 L310,280 L290,240 L250,210 Z',
    labelX: 255, labelY: 285,
    centers: ['Jaipur', 'Jodhpur', 'Udaipur'], partners: 12, distributors: 28,
  },
  {
    id: 'maharashtra', name: 'Maharashtra',
    path: 'M240,420 L220,460 L240,500 L280,510 L320,490 L340,450 L320,410 L280,400 Z',
    labelX: 285, labelY: 460,
    centers: ['Mumbai', 'Pune', 'Nagpur', 'Nashik', 'Aurangabad'], partners: 24, distributors: 56,
  },
  {
    id: 'mp', name: 'Madhya Pradesh',
    path: 'M300,300 L280,340 L290,380 L320,400 L360,390 L380,350 L370,310 L340,290 Z',
    labelX: 340, labelY: 350,
    centers: ['Indore', 'Bhopal', 'Gwalior'], partners: 10, distributors: 22,
  },
  {
    id: 'up', name: 'Uttar Pradesh',
    path: 'M340,220 L320,260 L330,300 L370,310 L410,300 L430,260 L420,220 L380,210 Z',
    labelX: 380, labelY: 265,
    centers: ['Lucknow', 'Kanpur', 'Agra', 'Varanasi', 'Noida'], partners: 15, distributors: 38,
  },
  {
    id: 'delhi', name: 'Delhi',
    path: 'M360,200 L350,215 L355,230 L370,235 L385,225 L380,205 Z',
    labelX: 370, labelY: 218,
    centers: ['New Delhi', 'Gurugram', 'Faridabad'], partners: 8, distributors: 15,
  },
  {
    id: 'haryana', name: 'Haryana',
    path: 'M320,190 L310,220 L330,240 L350,235 L360,210 L345,195 Z',
    labelX: 340, labelY: 215,
    centers: ['Chandigarh', 'Gurgaon'], partners: 6, distributors: 14,
  },
  {
    id: 'punjab', name: 'Punjab',
    path: 'M280,170 L270,200 L290,220 L310,210 L315,180 L300,165 Z',
    labelX: 298, labelY: 195,
    centers: ['Ludhiana', 'Amritsar', 'Jalandhar'], partners: 5, distributors: 12,
  },
  {
    id: 'karnataka', name: 'Karnataka',
    path: 'M260,520 L240,560 L260,600 L300,610 L340,580 L350,540 L320,510 Z',
    labelX: 300, labelY: 565,
    centers: ['Bangalore', 'Mysore', 'Hubli', 'Mangalore'], partners: 14, distributors: 35,
  },
  {
    id: 'ap', name: 'Andhra Pradesh',
    path: 'M340,480 L320,520 L340,560 L380,570 L420,550 L430,510 L410,480 L370,470 Z',
    labelX: 385, labelY: 520,
    centers: ['Hyderabad', 'Visakhapatnam', 'Vijayawada'], partners: 11, distributors: 26,
  },
  {
    id: 'telangana', name: 'Telangana',
    path: 'M320,450 L300,480 L320,510 L350,520 L370,490 L360,460 Z',
    labelX: 340, labelY: 485,
    centers: ['Hyderabad', 'Warangal'], partners: 9, distributors: 20,
  },
  {
    id: 'tamilnadu', name: 'Tamil Nadu',
    path: 'M310,580 L290,620 L310,660 L350,670 L380,640 L370,600 L340,580 Z',
    labelX: 345, labelY: 625,
    centers: ['Chennai', 'Coimbatore', 'Madurai', 'Salem'], partners: 16, distributors: 40,
  },
];

const otherStates = [
  { id: 'jk', name: 'J&K', path: 'M220,80 L200,120 L230,150 L260,130 L250,90 Z' },
  { id: 'hp', name: 'HP', path: 'M280,140 L270,165 L295,175 L305,155 Z' },
  { id: 'uk', name: 'UK', path: 'M330,160 L320,190 L345,200 L355,175 Z' },
  { id: 'bihar', name: 'Bihar', path: 'M460,280 L450,320 L480,340 L500,310 L490,275 Z' },
  { id: 'wb', name: 'WB', path: 'M480,350 L470,390 L500,410 L520,380 L510,355 Z' },
  { id: 'odisha', name: 'Odisha', path: 'M440,400 L430,440 L460,460 L480,430 L470,405 Z' },
  { id: 'chhattisgarh', name: 'CG', path: 'M380,380 L370,420 L400,440 L420,410 L410,385 Z' },
  { id: 'jharkhand', name: 'Jharkhand', path: 'M450,340 L440,370 L465,385 L480,360 L470,345 Z' },
  { id: 'goa', name: 'Goa', path: 'M240,505 L235,515 L245,520 L250,510 Z' },
  { id: 'kerala', name: 'Kerala', path: 'M260,610 L250,650 L270,670 L280,630 Z' },
];
---

<section class="py-20 sm:py-28 lg:py-36 bg-white" aria-labelledby="network-heading">
  <div class="container-main">
    <div class="text-center max-w-2xl mx-auto mb-14">
      <span class="section-label">
        <span class="w-8 h-px bg-brand-500"></span>
        Service Network
      </span>
      <h2 id="network-heading" class="text-3xl sm:text-4xl lg:text-5xl font-bold text-slate-900 mt-6 leading-tight">
        Service, <span class="text-gradient">Everywhere</span>
      </h2>
      <p class="mt-4 text-slate-500 text-base sm:text-lg">Pan-India network of service centers, partners, and distributors.</p>
    </div>

    <div class="grid lg:grid-cols-2 gap-10 lg:gap-16 items-start">
      <!-- Interactive Map -->
      <div class="relative">
        <div class="relative bg-slate-50 rounded-3xl border border-slate-100 p-4 sm:p-6 overflow-hidden" id="map-container">
          <!-- Legend -->
          <div class="flex items-center gap-4 mb-4 text-xs text-slate-500">
            <span class="flex items-center gap-1.5">
              <span class="w-3 h-3 rounded-sm bg-brand-500"></span> Active States
            </span>
            <span class="flex items-center gap-1.5">
              <span class="w-3 h-3 rounded-sm bg-slate-300"></span> Other States
            </span>
          </div>

          <!-- SVG Map -->
          <svg viewBox="0 0 600 700" class="w-full h-auto" aria-label="Interactive map of India showing Qbits service network">
            <rect width="600" height="700" fill="transparent" />

            <!-- Inactive states -->
            {otherStates.map((s) => (
              <path
                d={s.path}
                fill="#e2e8f0"
                stroke="#cbd5e1"
                stroke-width="1"
                class="transition-colors duration-300"
              />
            ))}

            <!-- Active states -->
            {states.map((s) => (
              <g class="map-state group cursor-pointer" data-state={s.id}>
                <path
                  d={s.path}
                  fill="#049D65"
                  stroke="#ffffff"
                  stroke-width="1.5"
                  class="transition-all duration-300 group-hover:fill-brand-600 group-hover:stroke-brand-400 group-hover:stroke-2"
                />
                <text
                  x={s.labelX}
                  y={s.labelY}
                  text-anchor="middle"
                  dominant-baseline="middle"
                  class="text-[10px] font-bold fill-white pointer-events-none opacity-90"
                >
                  {s.name}
                </text>
              </g>
            ))}
          </svg>

          <!-- Tooltip (inside map container) -->
          <div id="map-tooltip" class="absolute hidden z-20 bg-white rounded-2xl shadow-xl shadow-slate-900/10 border border-slate-100 p-4 w-64 pointer-events-none">
            <h4 id="tooltip-title" class="text-base font-bold text-slate-900 mb-1"></h4>
            <p class="text-xs text-slate-400 mb-3">Qbits Service Network</p>
            <div class="mb-3">
              <p class="text-[10px] font-semibold uppercase tracking-wider text-slate-400 mb-1">Service Centers</p>
              <div id="tooltip-centers" class="flex flex-wrap gap-1"></div>
            </div>
            <div class="flex gap-4 pt-3 border-t border-slate-100">
              <div>
                <p id="tooltip-partners" class="text-lg font-bold text-brand-600"></p>
                <p class="text-[10px] text-slate-500">Partners</p>
              </div>
              <div>
                <p id="tooltip-distributors" class="text-lg font-bold text-brand-600"></p>
                <p class="text-[10px] text-slate-500">Distributors</p>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Right Content -->
      <div class="grid grid-cols-2 gap-5">
        <div class="p-6 rounded-2xl bg-slate-50 border border-slate-100">
          <p class="text-3xl sm:text-4xl font-bold text-brand-600 mb-1">25+</p>
          <p class="text-sm text-slate-600 font-medium">States Covered</p>
        </div>
        <div class="p-6 rounded-2xl bg-slate-50 border border-slate-100">
          <p class="text-3xl sm:text-4xl font-bold text-brand-600 mb-1">100+</p>
          <p class="text-sm text-slate-600 font-medium">Service Partners</p>
        </div>
        <div class="p-6 rounded-2xl bg-slate-50 border border-slate-100">
          <p class="text-3xl sm:text-4xl font-bold text-brand-600 mb-1">500+</p>
          <p class="text-sm text-slate-600 font-medium">Distributors</p>
        </div>
        <div class="p-6 rounded-2xl bg-slate-50 border border-slate-100">
          <p class="text-3xl sm:text-4xl font-bold text-brand-600 mb-1">&lt;48h</p>
          <p class="text-sm text-slate-600 font-medium">Response Time</p>
        </div>

        <!-- States list -->
        <div class="col-span-2 mt-4">
          <p class="text-sm font-semibold text-slate-900 mb-3">Active States</p>
          <div class="flex flex-wrap gap-2">
            {states.map((s) => (
              <span class="px-3 py-1.5 rounded-full bg-slate-50 border border-slate-200 text-xs font-medium text-slate-600 hover:border-brand-300 hover:text-brand-600 transition-colors cursor-default">
                {s.name}
              </span>
            ))}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  const stateData = {
    gujarat: { name: 'Gujarat', centers: ['Surat', 'Ahmedabad', 'Vadodara', 'Rajkot'], partners: 18, distributors: 42 },
    rajasthan: { name: 'Rajasthan', centers: ['Jaipur', 'Jodhpur', 'Udaipur'], partners: 12, distributors: 28 },
    maharashtra: { name: 'Maharashtra', centers: ['Mumbai', 'Pune', 'Nagpur', 'Nashik', 'Aurangabad'], partners: 24, distributors: 56 },
    mp: { name: 'Madhya Pradesh', centers: ['Indore', 'Bhopal', 'Gwalior'], partners: 10, distributors: 22 },
    up: { name: 'Uttar Pradesh', centers: ['Lucknow', 'Kanpur', 'Agra', 'Varanasi', 'Noida'], partners: 15, distributors: 38 },
    delhi: { name: 'Delhi NCR', centers: ['New Delhi', 'Gurugram', 'Faridabad'], partners: 8, distributors: 15 },
    haryana: { name: 'Haryana', centers: ['Chandigarh', 'Gurgaon'], partners: 6, distributors: 14 },
    punjab: { name: 'Punjab', centers: ['Ludhiana', 'Amritsar', 'Jalandhar'], partners: 5, distributors: 12 },
    karnataka: { name: 'Karnataka', centers: ['Bangalore', 'Mysore', 'Hubli', 'Mangalore'], partners: 14, distributors: 35 },
    ap: { name: 'Andhra Pradesh', centers: ['Hyderabad', 'Visakhapatnam', 'Vijayawada'], partners: 11, distributors: 26 },
    telangana: { name: 'Telangana', centers: ['Hyderabad', 'Warangal'], partners: 9, distributors: 20 },
    tamilnadu: { name: 'Tamil Nadu', centers: ['Chennai', 'Coimbatore', 'Madurai', 'Salem'], partners: 16, distributors: 40 },
  };

  const mapStates = document.querySelectorAll('.map-state');
  const tooltip = document.getElementById('map-tooltip');
  const tooltipTitle = document.getElementById('tooltip-title');
  const tooltipCenters = document.getElementById('tooltip-centers');
  const tooltipPartners = document.getElementById('tooltip-partners');
  const tooltipDistributors = document.getElementById('tooltip-distributors');
  const mapContainer = document.getElementById('map-container');

  function showTooltip(stateId, x, y) {
    const data = stateData[stateId];
    if (!data || !tooltip) return;

    tooltipTitle.textContent = data.name;
    tooltipPartners.textContent = data.partners;
    tooltipDistributors.textContent = data.distributors;
    tooltipCenters.innerHTML = data.centers.map((c) =>
      `<span class="px-2 py-0.5 rounded-full bg-brand-50 text-brand-600 text-[10px] font-medium">${c}</span>`
    ).join('');

    tooltip.classList.remove('hidden');

    // Position tooltip near the cursor but keep inside container
    const containerRect = mapContainer.getBoundingClientRect();
    let left = x - containerRect.left + 16;
    let top = y - containerRect.top + 16;

    // Prevent overflow
    const tooltipRect = tooltip.getBoundingClientRect();
    if (left + tooltipRect.width > containerRect.width) {
      left = x - containerRect.left - tooltipRect.width - 16;
    }
    if (top + tooltipRect.height > containerRect.height) {
      top = y - containerRect.top - tooltipRect.height - 16;
    }

    tooltip.style.left = left + 'px';
    tooltip.style.top = top + 'px';
  }

  function hideTooltip() {
    tooltip?.classList.add('hidden');
  }

  mapStates.forEach((el) => {
    el.addEventListener('mouseenter', (e) => {
      const stateId = el.getAttribute('data-state');
      if (stateId) showTooltip(stateId, e.clientX, e.clientY);
    });
    el.addEventListener('mousemove', (e) => {
      const stateId = el.getAttribute('data-state');
      if (stateId) showTooltip(stateId, e.clientX, e.clientY);
    });
    el.addEventListener('mouseleave', () => {
      hideTooltip();
    });
  });
</script>
